Practice for git.
git add/git commit: to save project to git
git branch, git checkout, git switch: to view and switch between branches
git remote add origin (link here): to connect via ssh/https
git push -u origin main: to push main branch to origin
